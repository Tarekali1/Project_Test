#include <stdio.h>
#include <stdlib.h>
#include "Test1.h"
int main()
{
    int x = 10 ;
    int y = 20 ;
    printf("sum of %d & %d = %d  \n",x,y,Sum_values(x,y));
    printf("sum of %d & %d = %d  \n",x,y,Multy(x,y));
    return 0;

}
